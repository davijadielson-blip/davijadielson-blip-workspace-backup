---
tipo: diagnostico
data: 2026-05-17
gerado-por: claude-code
revisado: false
escopo: frentes-1-a-5
---

# Diagnóstico do Sistema — 2026-05-17

> Sessão de investigação pura — sem correções aplicadas.
> Investigador: Claude Sonnet 4.6 · Vault v3.1

---

## Frente 1 — Crons e Notificações

### Achados

| Item | Status | Detalhe |
|---|---|---|
| Agentes launchd ativos | ✅ | 3 jadielson (daily-brief, friday-maintenance, saturday-planning) + 1 telegram-polling |
| daily-brief | ⚠️ | Roda com erro no git; notificação chega Telegram + email |
| saturday-planning | ⚠️ | Log vazio (crash no git); notificação chega. `runs=0` no launchctl mas .err de 16/mai existe |
| friday-maintenance | ✅ | Limpo, sem erros, log populado |
| telegram-polling | ✅ | 204 execuções, offset ativo |
| 13h/17h reminders | 🔴 | Plists inexistentes — funcionalidade nunca implementada |

### Root cause
**`fatal: mmap failed: Resource deadlock avoided`** em `daily-brief.err` e `saturday-planning.err`.
OneDrive daemon mantém file locks em `.git/objects` durante `git commit/push` dentro dos crons.
Afeta daily-brief (esporadicamente) e saturday-planning (sistematicamente).

### Duração medida
- daily-brief: 29.8s
- saturday-planning: 5.7s (crash rápido)
- friday-maintenance: 6.6s
- telegram-polling: 5.7s por ciclo

### Correção sugerida (não aplicada)
```bash
mv "$VAULT/.git" "$HOME/.mapa-obsidian-git"
ln -s "$HOME/.mapa-obsidian-git" "$VAULT/.git"
```
Mover `.git` para fora do escopo do OneDrive via symlink.

---

## Frente 2 — Sync Notion → Google Calendar

### Achados

| Item | Status | Detalhe |
|---|---|---|
| Credenciais Google | ✅ | token + refresh_token; auto-refresh funciona |
| Notion token | ✅ | `NOTION_SYNC_TOKEN` do notion.env coincide com hardcoded no script |
| Câmara no script | 🔴 | ID comentado — frente nunca sincroniza |
| SINDSS no script | 🔴 | ID comentado — frente nunca sincroniza |
| Captura Geral hoje | 🔴 | `HTTP Error 404: Not Found` — banco `242f2506...` inacessível |
| Captura Geral 15–16/mai | ✅ | Funcionou: 15/mai 1+4, 16/mai 1+5 |
| Editorial Saúde | ⚠️ | 21 itens — todos de abril/2026, nenhum de maio |
| Duração anômala | ⚠️ | 12/mai: 979s · 14/mai: 1285s (normal: ~15s) — provável timeout com retry |
| Bug all-day event | ⚠️ | `end = start` para eventos all-day — Google Calendar exige `end = start + 1 dia` |

### Root causes
1. **404 Captura Geral**: `NOTION_CAPTURA_DATABASE_ID = 242f2506...` funcionou até 16/mai, 404 em 17/mai — integração perdeu acesso ou banco foi movido/deletado no Notion
2. **Câmara + SINDSS**: IDs nunca foram inseridos no script — comentários "colar ID quando criar espaço"
3. **Itens de abril**: conteúdo de maio não tem "Data da Publicacao" preenchida no Notion Saúde, ou ainda não foi criado
4. **Duas origens do token Notion**: `parse-captura.py` usa `NOTION_TOKEN` (ntn_H872...) enquanto `notion-to-calendar.py` usa token hardcoded = `NOTION_SYNC_TOKEN` (ntn_b872...) — dois tokens diferentes, potencial inconsistência futura

---

## Frente 3 — Formatação Inbox

### Achados

| Item | Status | Detalhe |
|---|---|---|
| Plugin `properties` Obsidian | ⚠️ | `false` — frontmatter criado por parse-captura aparece como bloco `---` bruto |
| Naming novos (parse-captura) | ✅ | `YYYY-MM-DD-HHMM-slug.md` correto |
| Naming antigos (2025) | ⚠️ | 20+ arquivos em MAIÚSCULAS sem data — padrão pré-sistema |
| Frontmatter novos | ✅ | `tipo + data + hora + origem` preenchidos |
| Frontmatter antigos | 🔴 | Sem frontmatter — texto puro |
| `PEDIDO DE ARTE...` | 🔴 | 0 bytes + `rwx------` — arquivo executável vazio |
| PDF inbox | ⚠️ | `Extrutura base da secretaria.pdf` (228KB) — não é nota |
| parse-captura → Notion | 🔴 | Usa mesmo `NOTION_CAPTURA_DATABASE_ID` com 404 — capturas com prefixo falham silenciosamente |

### Root causes
1. **404 afeta dois pipelines**: sync E parse-captura — todo `/t /c /r /g /n /p` enviado pelo Telegram está sendo descartado sem feedback
2. **Backlog de arquivos pré-sistema**: não é bug, é triagem manual pendente
3. **Plugin properties desligado**: decisão de UX — sem impacto funcional

---

## Frente 4 — Bot Telegram

### Achados

| Item | Status | Detalhe |
|---|---|---|
| Polling ativo | ✅ | 204 execuções, offset `282989740` |
| `/tmp` logs | ⚠️ | Ambos vazios — nenhuma mensagem processada nas últimas execuções |
| Confirmação de volta | 🔴 | `send-telegram.sh` existe mas nunca é chamado no fluxo de captura |
| Texto puro (sem `/`) | ⚠️ | É processado (lógica: `startswith("/") and not is_capture_cmd`) — vai para Notion → 404 |
| `/start` e comandos Telegram | ✅ | Corretamente ignorados |
| `/i` e `/inbox` | ✅ | Cria nota no vault — funciona independente do Notion |
| `/t /c /r /g /n /p` | 🔴 | Dependem de Notion → 404 → descartados silenciosamente |
| Erro visível ao usuário | 🔴 | Nenhum — falha no subprocess não chega ao remetente |

### Root causes
1. **Sem confirmação**: depois de `subprocess.run(parse-captura.py)`, nenhuma chamada a `send-telegram.sh`
2. **Dependência do Notion (404)**: 6 dos 8 prefixos afetados
3. **Falha silenciosa**: erro só aparece em log local — usuário não sabe que a captura foi descartada

### O que funciona hoje
- `/i minha ideia` → cria em `[F0] 0-Inbox/` ✅
- Notificações de cron (Telegram + email) ✅

---

## Frente 5 — Estado Geral

### Orphan Drafts ([F2] memory/outputs/)

| Arquivo | Idade | Diagnóstico |
|---|---|---|
| Legenda 1-3 Visita ao Salobro | 9d | Provavelmente obsoletos |
| `2026-05-08-saude-novo-numero-emulti.md` | 8d | Possivelmente obsoleto |
| `CLAUDE-proposto.md` | 7d | Relic — versão v1.0 (atual v3.1) |
| `_MAP.md` | 6d | Mapa de outputs — lugar discutível |

### Hooks
- Todos 4 scripts existem: `session-start.sh`, `prompt-guard.sh`, `post-write.sh`, `session-log.sh`
- Configurados em `settings.json` ✅
- `settings.local.json` tem `hooks: {}` — não interfere

### Git status
| Item | Status |
|---|---|
| Sync logs 12–16/mai | 🔴 5 arquivos nunca commitados |
| `scripts/lib/time-blocking.py` | ⚠️ Novo script não commitado (243 linhas) |
| `[F2] memory/databases/prompts-pack/` | ⚠️ Nova pasta não commitada |
| `REBASE_HEAD` + `ORIG_HEAD` | ⚠️ Rebase anterior concluído, refs não limpos |
| "Sem título" canvas/base (11 arquivos) | ⚠️ Artefatos Obsidian no root — não commitados |
| `2026-05-17.md` no root | ⚠️ 0 bytes, hoje — daily note no lugar errado |

### OneDrive
- 0 arquivos `.cloudd` em `.git/` — tudo local
- xattr `com.apple.provenance` é metadata Apple, não OneDrive
- Deadlock acontece no nível de processo do daemon, não por deferred download

---

## Resumo de Prioridades

| # | Problema | Impacto | Urgência |
|---|---|---|---|
| 1 | 404 Captura Geral (Notion) | Capturas Telegram falhando, sync Captura inativo | 🔴 Crítica |
| 2 | Sem confirmação no bot Telegram | Usuário envia, não recebe feedback | 🔴 Alta |
| 3 | OneDrive .git lock (mmap) | daily-brief e saturday-planning com erro no git | 🟡 Média |
| 4 | Câmara + SINDSS sem ID no sync | Calendário editorial incompleto | 🟡 Média |
| 5 | 5 sync logs não commitados | Histórico incompleto | 🟢 Baixa |
| 6 | Backlog inbox pré-sistema | Organização | 🟢 Baixa |
| 7 | Artefatos Obsidian no root | Ruído no vault | 🟢 Baixa |

---

*Gerado automaticamente em 2026-05-17 · Sessão diagnóstica pura — nenhuma correção aplicada.*
