---
tema: memorias duraveis de 2026-07-29
conteudo: registros operacionais do dia, recuperacao do Cofre, ajustes de agentes e WhatsApp
setor: operacoes tecnicas
cliente: Jadielson Davi
tipo: memoria diaria
prioridade: alta
atualizado_em: 2026-07-29
usar_quando: consultar continuidade operacional e decisoes registradas em 2026-07-29
nao_usar_quando: buscar regras permanentes ou contexto autoral do Jadielson
---

# Memórias duráveis — 2026-07-29

## Flush pré-compactação — modelos/agentes OpenClaw

- Jadielson relatou que Lôh roda pelo Codex, mas outros agentes/subagentes estavam caindo no OpenRouter e consumindo créditos. Diagnóstico inicial: configuração misturava `openai-codex/gpt-5.5`, `openai/gpt-5.1-codex` e fallbacks pagos OpenRouter.
- Política desejada por Jadielson: manter `openai-codex/gpt-5.5` como primário, mas preservar fallback OpenRouter como planos B/C para garantir operação mínima quando Codex não rodar por limitação. Não bloquear OpenRouter; ordenar por custo.
- Sequência de fallback aprovada durante a conversa: primário `openai-codex/gpt-5.5`; fallback começando por `openrouter/deepseek/deepseek-v4-flash` por ser mais barato; depois alternativas OpenRouter conforme configuração disponível.
- Foi observado que um agente em tópico novo (`https://t.me/c/3645702069/8361`) mostrou: `Model Fallback: openrouter/deepseek/deepseek-v4-flash (selected openai-codex/gpt-5.5; model not found)`. Isso indica que alguns agentes/subagentes ainda não reconhecem corretamente o modelo `openai-codex/gpt-5.5` ou herdam configuração antiga/cache, mesmo após ajustes globais.
- Também houve erro em agente: `API provider returned a billing error — your API key has run out of credits or has an insufficient balance`, compatível com fallback OpenRouter pago sem saldo suficiente.
- Correções já tentadas no fluxo: alinhar defaults, subagents e agentes explícitos para `openai-codex/gpt-5.5`; ajustar fallbacks OpenRouter; recarregar Gateway; registrar decisões no Cofre; fazer commits/pushes. Tentativa de apontar agente para `agentDir` do main foi revertida porque provocou fallback indevido/antigo.
- Próxima continuidade: auditar `/data/.openclaw/openclaw.json`, diretórios de agentes em `/data/.openclaw/agents/*/agent/`, sessões/logs do tópico novo e qualquer cache/runtime que ainda traduza `openai-codex/gpt-5.5` como “model not found”. Validar com teste real no agente/tópico afetado antes de declarar resolvido.

## Flush pré-compactação — dúvida de custo de modelo

- Jadielson perguntou se “esse modelo” gasta créditos da API, no contexto da correção de modelos/fallbacks. Continuidade: responder com distinção clara entre `openai-codex/gpt-5.5` e OpenRouter.
- Entendimento operacional a preservar: `openai-codex/gpt-5.5` deve rodar pelo provedor Codex/OpenAI configurado na Lôh; não deve consumir créditos OpenRouter. Já os fallbacks `openrouter/...`, inclusive `openrouter/deepseek/deepseek-v4-flash`, consomem saldo/créditos do OpenRouter quando acionados. Se um agente mostra `Model Fallback: openrouter/...`, significa que já saiu do Codex e entrou no OpenRouter.
- Resposta recomendada ao Jadielson: “Codex GPT-5.5 não deve gastar seus créditos OpenRouter; o DeepSeek/OpenRouter gasta. O gasto só acontece quando o fallback aparece/é usado. O problema atual é fazer todos reconhecerem o Codex para evitar fallback indevido.”

## Auditoria heartbeat — modelo canônico corrigido

- Verificação em 2026-07-29 00:33 UTC: `/data/.openclaw/openclaw.json`, `openclaw config get agents.defaults --json`, `openclaw models status --json` e `openclaw agents list --json` estão convergentes para `openai/gpt-5.5` como primário oficial, com runtime Codex e OAuth OpenAI utilizável.
- `agents.defaults.model.primary`, `agents.defaults.subagents.model.primary` e 21/21 agentes listados estão em `openai/gpt-5.5`; fallbacks preservados em ordem econômica: `openrouter/deepseek/deepseek-v4-flash`, depois `openrouter/minimax/minimax-m2.5:free`.
- Conclusão operacional atual: o ID que deve ser usado no Gateway/OpenClaw é `openai/gpt-5.5`. O antigo `openai-codex/gpt-5.5` deve ser tratado como nomenclatura histórica/legada, pois aparece nos rastros de `model not found` que acionavam fallback OpenRouter.
- Observação: há rastros antigos em `sessions.json` com `fallbackNoticeSelectedModel: openai-codex/gpt-5.5`; eles parecem histórico de sessões anteriores, não configuração ativa. Para validar em produção, testar uma nova mensagem no tópico afetado `8361` e confirmar ausência de banner `Model Fallback`.

## Flush pré-compactação — dúvida sobre créditos OpenAI

- Jadielson refinou a pergunta: “me refiro se gasta créditos na OpenAI”, isto é, não apenas créditos OpenRouter. Continuidade: responder com cuidado e sem afirmar além do verificado.
- Contexto técnico atual: o modelo canônico ativo passou a ser `openai/gpt-5.5` com runtime/Codex OAuth, não `openai-codex/gpt-5.5`. Isso pode indicar uso vinculado à autenticação OpenAI/Codex da conta, mas é necessário verificar no ambiente/conta se o consumo vai para plano Codex/ChatGPT ou para billing/API OpenAI.
- Resposta segura recomendada: “OpenRouter só cobra quando aparece `openrouter/...`. Sobre OpenAI: se estiver rodando via `openai/gpt-5.5` com API key/billing, pode consumir créditos da OpenAI; se estiver rodando pelo runtime Codex/OAuth da conta, tende a contar no acesso/limite do Codex/ChatGPT, não no saldo OpenRouter. Vou checar no Gateway/logs antes de afirmar 100%.”
- Próxima ação: auditar `auth-profiles.json`/logs do provider usado pelo `main` e um agente novo para identificar se o provider é tratado como API billing OpenAI ou Codex OAuth, sem expor tokens.

## Governanca de memoria e limpeza do OpenClaw

- Jadielson aprovou uma politica de governanca para manter agentes OpenClaw organizados, coerentes e com armazenamento sob controle.
- Principio central registrado: o Cofre e a fonte oficial da verdade; sessoes sao temporarias e nao devem funcionar como memoria permanente.
- Regra operacional: consolidar antes de limpar. Decisoes, preferencias, processos, status, proximos passos, responsabilidades, prazos e aprendizados reutilizaveis devem ser registrados no Cofre antes do encerramento de tarefas/sessoes.
- Conteudos sem valor permanente, como saudacoes, agradecimentos, testes, repeticoes, respostas intermediarias, logs sem utilidade futura e detalhes temporarios, nao devem contaminar o Cofre.
- A limpeza diaria apos 00h deve diagnosticar disco, identificar temporarios, confirmar consolidacao no Cofre, preservar sessoes ativas/recentes/pendentes, agir de forma seletiva e produzir relatorio.
- Protecao reforcada: nao apagar automaticamente workspace, memory, configuracoes, credenciais, prompts, instrucoes dos agentes, bancos, backups, projetos, documentos oficiais, arquivos de decisao, sessoes ativas ou sessoes com pendencias. Em duvida, marcar como revisao necessaria/quarentena.
- Processo oficial criado em `60-processos/governanca-memoria-limpeza-openclaw.md` e decisao registrada em `00-central/decisoes.md`.
- Primeiro diagnostico salvo em `60-processos/relatorios/limpeza-openclaw/2026-07-29-0147-diagnostico.md`: disco em 99% (`/data` 9.9G/10G, 107M livres), `/data/.openclaw/agents` com 5.2G e sessoes antigas de agentes (+3 dias) somando cerca de 1.7G. Nenhuma exclusao foi executada; itens ficaram para revisao/acao segura.

## Recuperacao do Cofre e reativacao do ecossistema

- O Cofre completo foi recuperado do repositorio de backup correto `davijadielson-blip/davijadielson-blip-workspace-backup`, commit `f2224f2b9893682f6fedd0f34fc98f0d28239fb4` de 2026-07-29 01:54 UTC. O backup foi validado com `git fsck --full --strict` antes e depois da restauracao.
- Antes da restauracao foi preservado o estado integral em `/data/openclaw-backups/pre-cofre-restore-20260729T0430Z.tar.gz`. A restauracao foi feita por sobreposicao, sem exclusao, mantendo os materiais produzidos durante o incidente.
- O estado restaurado voltou a conter a arvore canonica `00-central/` a `90-arquivo/`, `memory/` e `.git`, totalizando aproximadamente 5.055 arquivos, 2.247 diretorios e 104 MB.
- A causa operacional da ausencia dos agentes nao estava no Cofre: `/data/.openclaw/openclaw.json` ja havia sido reduzido a apenas `main`, sem bindings, antes da restauracao. O snapshot canonico usado para recuperacao foi `90-arquivo/99-quarentena-nao-md/openclaw-config-canonical-agents-2026-06-26.json`.
- Antes de religar os agentes foi criado o backup `/data/.openclaw/openclaw.json.bak-before-agent-relink-20260729T0500Z`.
- Foram reativados 20 agentes com o modelo oficial `openai/gpt-5.5`, quatro grupos do Telegram e 33 topicos: 22 na LOGIKA, 9 na Central Pessoal, 1 em Estudos e 1 em Projetos. Tres bindings de grupo foram restaurados: LOGIKA para Jarvis; Estudos e Projetos para `central-topic-agent`.
- Validacao final: `openclaw config validate --json` retornou `valid: true`; nenhum topico aponta para agente inexistente; Gateway e Telegram estao saudaveis. Testes internos responderam corretamente com Jarvis, CRO, Alfred e agente tematico, e sessoes reais foram abertas por roteamento.
- A politica de agentes dormentes permanece ativa em `agents.defaults.heartbeat.every = "0m"`. Placeholders gerados automaticamente foram preservados, sem exclusao, em `70-agentes/runtime/_quarantine/generated-placeholders-20260729/`.
- Registro operacional detalhado: `memory/sessions/2026-07-29.md`.

## Otimização de latência do WhatsApp

- Jadielson informou que as respostas pelo WhatsApp estavam demorando muito.
- Medição no transcript da sessão compartilhada mostrou cerca de 56 segundos para uma saudação simples e 3 minutos e 15 segundos para a pauta do dia.
- O WhatsApp estava conectado e saudável; o gargalo principal era o processamento da sessão `main`, compartilhada com o Telegram, com raciocínio em nível alto e contexto grande.
- Ajustes aplicados em `/data/.openclaw/openclaw.json`:
  - `session.dmScope = "per-channel-peer"` para separar as sessões diretas do Telegram e WhatsApp;
  - `agents.list[0].thinkingDefault = "low"` para respostas cotidianas mais rápidas da Lôh;
  - `channels.whatsapp.ackReaction = {"emoji":"👀","direct":true,"group":"mentions"}` para confirmar imediatamente o recebimento.
- Configuração validada sem alertas e Gateway recarregado. WhatsApp permaneceu `linked`, `connected`, `healthy`, sem erro ou tentativa de reconexão.
- Backup anterior preservado em `/data/.openclaw/openclaw.json.bak-before-whatsapp-latency-20260729T0612Z`.

## Teste de grupo no WhatsApp

- Jadielson pediu orientação para permitir acesso da Lôh a um grupo do WhatsApp.
- O Gateway recebeu mensagens novas do grupo `120363429699071256@g.us` em 2026-07-29 por volta de 06:33 UTC, enviadas pelo número autorizado de Jadielson.
- O WhatsApp estava `linked`, `connected`, `healthy`, com `groupPolicy = allowlist`, `groupAllowFrom` limitado aos números de Jadielson e reação imediata `👀` configurada para grupos quando houver menção.
- Uma mensagem de confirmação foi enviada manualmente ao grupo via `openclaw message send --channel whatsapp --target 120363429699071256@g.us`.
- Diagnóstico de latência do teste: entrada no grupo em 06:33:49 UTC e saída em 06:34:01 UTC, cerca de 12 segundos na última resposta observada; o transporte do WhatsApp não apresentou erro.
- Jadielson informou que `+5582998130506` também é número dele. O número foi adicionado em `/data/.openclaw/openclaw.json` a `channels.whatsapp.allowFrom` e `channels.whatsapp.groupAllowFrom`, mantendo `dmPolicy` e `groupPolicy` em `allowlist`.
- Backup criado antes da alteração: `/data/.openclaw/openclaw.json.bak-before-whatsapp-number-20260729T1109Z`. Validação `openclaw config validate --json` retornou `valid: true`. Status posterior do WhatsApp: `linked`, `connected`, `healthy`, com o novo número aparecendo em `allowFrom`.
- Após Jadielson testar novamente pelo outro número, os logs do WhatsApp não registraram nova mensagem inbound após 06:33 UTC. Hipótese principal: adicionar o número à allowlist autoriza o remetente, mas não conecta esse outro WhatsApp; o WhatsApp efetivamente conectado ao Gateway continua sendo `+558291468088`, que precisa estar dentro do grupo para receber as mensagens.
- Diagnóstico posterior encontrou que o histórico do grupo renderizava o outro participante como `+558298130506`, sem o nono dígito, além do número informado `+5582998130506`. Essa variação também foi adicionada a `channels.whatsapp.allowFrom` e `channels.whatsapp.groupAllowFrom`.
- O grupo `120363429699071256@g.us` foi registrado explicitamente em `channels.whatsapp.groups` com `requireMention: true`. Validação da configuração retornou `valid: true`.
- Mudanças em `channels.whatsapp.*` não aplicaram por hot reload; foi necessário reiniciar o processo `openclaw-gateway`. Após restart real, logs passaram a mostrar `Listening for WhatsApp inbound messages (DM + 1 configured group)` e status JSON mostrou allowlist ativa com quatro entradas: `+5582991468088`, `+558291468088`, `+5582998130506`, `+558298130506`.
- Mensagem técnica enviada ao grupo via Gateway em 2026-07-29 11:32 UTC para solicitar novo teste de inbound: `3EB0F87178E588C0193D1F`.
- Teste confirmado por Jadielson como funcional em 2026-07-29 12:31 UTC. Logs posteriores registraram inbound do grupo `120363429699071256@g.us` a partir de 11:34 UTC, reação `👀` imediata e respostas enviadas pelo WhatsApp.

## Constituicao definitiva de memoria, backup e limpeza segura

- Jadielson enviou a Constituicao do Ecossistema Lôh com missao de tornar o ambiente resiliente, economico, auditavel e capaz de preservar conhecimento sem depender de sessoes antigas.
- Auditoria inicial confirmou que o Cofre atual possui a estrutura canonica `00-central/` a `90-arquivo/` e `memory/`, os arquivos raiz exigidos, Git remoto oficial `davijadielson-blip/davijadielson-blip-workspace-backup` em `origin/main`, OpenClaw config valida, 20 agentes ativos e disco em 51%.
- Nao havia cron ativo no escopo atual. Foi criado o job OpenClaw `governanca-cofre-sessoes-diaria-0000`, ID `df970ab7-4083-433f-b007-b34e6c68d130`, para rodar diariamente as 00h00 no timezone `America/Maceio`.
- Modo inicial do job: auditoria/simulacao conservadora, consolidacao no Cofre, checagem de segredos, backup seletivo se seguro, confirmacao de hash remoto, relatorio compacto e nenhuma exclusao permanente.
- Relatorio inicial salvo em `60-processos/relatorios/limpeza-openclaw/2026-07-29-1256-auditoria-constituicao-loh.md`; decisao consolidada em `00-central/decisoes.md`; processo atualizado em `60-processos/governanca-memoria-limpeza-openclaw.md`.
- Commit local criado para a implementacao inicial. Push remoto falhou por falta de credencial GitHub HTTPS no container; pendencia: restaurar credencial de push antes de permitir qualquer limpeza que dependa de backup confirmado.
